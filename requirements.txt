# Production dependencies for the lean Vercel function.
# Optional desktop, document, search, and vision libraries stay out of the
# deployment bundle; local development can install them separately.
flask==3.1.3
flask-sock==0.7.0
flask-cors==6.0.2
requests==2.32.5
python-dotenv==1.0.1
edge-tts==7.2.8
# PostgreSQL driver for the connected Neon database; SQLite remains the local fallback.
psycopg[binary]==3.2.9
